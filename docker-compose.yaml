services:

  mongodb:
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"
    environment:
      - "MONGO_INITDB_ROOT_USERNAME=${DB_NAME}"
      - "MONGO_INITDB_ROOT_PASSWORD=${DB_PASS}"
